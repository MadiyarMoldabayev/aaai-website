#!/bin/bash

# Автоматический деплой на Netlify
echo "🚀 Начинаю деплой на Netlify..."

# Создаем новый сайт и деплоим
echo "2" | netlify init --manual=false 2>&1 | head -20

# Если сайт уже создан, просто деплоим
if [ -f ".netlify/state.json" ]; then
    echo "✅ Сайт уже настроен, выполняю деплой..."
    netlify deploy --prod --dir=.
else
    echo "📦 Создаю новый сайт..."
    # Используем интерактивный режим
    netlify init
fi

echo "✅ Деплой завершен!"

